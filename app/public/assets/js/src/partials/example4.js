/*
	Example module 4
	------------------------------- */

	define(['jquery'], function($) {

		'use strict';

		// Test jQuery dependency
		if ($ && window.console)
			console.log('Example 4 has loaded');
	});
