/*
	App launcher
	------------------------------- */

	define('app', ['loadCSS', 'onloadCSS', 'loadJS', 'example1'], function() {

		'use strict';

		if (window.console)
			console.log('Start application');
	});
