/*
	Example module 2
	------------------------------- */

	define(['jquery'], function($) {

		'use strict';

		// Test jQuery dependency
		if ($ && window.console)
			console.log('Example 2 has loaded');
	});
